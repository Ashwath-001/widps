[Unit]
Description=WIDPS Wireless Intrusion Detection & Prevention System
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/home/ashh/widps
ExecStartPre=-/usr/sbin/airmon-ng start wlan1
ExecStart=/home/ashh/widps/widps-backend/target/release/widps
Restart=always
RestartSec=5
TimeoutStopSec=10

# Environment
Environment=RUST_LOG=info
Environment=WIDPS_SIEM_ENABLED=1
Environment=WIDPS_SIEM_HOST=192.168.1.100
Environment=WIDPS_SIEM_PORT=1514
Environment=WIDPS_SIEM_FORMAT=json
Environment=WIDPS_SIEM_TCP=0

# Resource limits
LimitNOFILE=65536

# Logging
StandardOutput=journal
StandardError=journal
SyslogIdentifier=widps

[Install]
WantedBy=multi-user.target
